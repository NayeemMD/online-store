/*
var expect  = require('chai').expect;
var request = require('request');

it('Check Ping page', function(done) {
    request('http://localhost:2300/ping' , function(error, response, body) {
        console.log(body);
        expect(body).to.equal('pong!');
        done();
    });
});*/
